repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
- name: redis
  namespace: default
  chart: bitnami/redis
  version: 11.3.4
  values:
  - "values.yaml.gotmpl"

environments:
  default:
    values:
      - ./../../../../private/helm/default.yaml
