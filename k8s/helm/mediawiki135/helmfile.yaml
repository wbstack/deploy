releases:
- name: mediawiki-135
  namespace: default
  chart: ./../../../charts/mediawiki
  values:
  - "values.yaml.gotmpl"

environments:
  default:
    values:
      - ./../default.yaml
      - ./../../../../private/helm/private.yaml
