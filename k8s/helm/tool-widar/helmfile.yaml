releases:
- name: tool-widar
  namespace: default
  chart: ./../../../charts/tool-widar
  values:
    - "values.yaml.gotmpl"

environments:
  default:
    values:
      - ./../default.yaml
      - ./../../../../private/helm/private.yaml
