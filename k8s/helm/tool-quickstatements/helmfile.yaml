releases:
- name: tool-quickstatements
  namespace: default
  chart: ./../../../charts/tool-quickstatements
  values:
    - "values.yaml.gotmpl"

environments:
  default:
    values:
      - ./../../../../private/helm/default.yaml
